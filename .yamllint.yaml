extends: default

ignore: server/job.tpl.yaml

rules:
  line-length: disable
  document-start: disable
  indentation:
    spaces: consistent
    indent-sequences: consistent
  truthy:
    allowed-values: ['true', 'false']
    check-keys: true
